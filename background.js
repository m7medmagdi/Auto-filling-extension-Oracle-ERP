// Background script to handle persistent popup (if needed)
chrome.action.onClicked.addListener((tab) => {
    // This ensures the popup remains accessible, but persistence is mostly browser-controlled
  });